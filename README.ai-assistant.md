# AI assistant scaffold

This commit adds a minimal scaffold (frontend + backend) to start building the assistant.

Next steps:
- Provide Gemini/OpenAI API key in repo secrets or env as GEMINI_API_KEY/OPENAI_API_KEY
- Run infra/docker-compose or start frontend/backend individually
